# URL pública do backend (App Runner). Em produção (Amplify), defina esta var
# no console do Amplify. Em dev local deixe em branco para usar o proxy do Vite.
VITE_API_BASE_URL=https://xxxxxxxx.us-east-1.awsapprunner.com/api
